# Leia total (float) e pessoas (int). Use /= para dividir igualmente.

total = float(input('Digite o total: '))
pessoas = float(input('Digite o número de pessoas: '))

total /= pessoas

print('Resultado:', total)